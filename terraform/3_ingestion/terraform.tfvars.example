# Part 3: Ingestion Pipeline Configuration
# Copy this file to terraform.tfvars and update with your values

# Your AWS region for the ingestion infrastructure
# Should match your DEFAULT_AWS_REGION from .env
aws_region = "us-east-1"

# Bedrock embedding model ID
# Using Titan Embeddings v2 for high-quality 1536-dimension embeddings
bedrock_embedding_model = "amazon.titan-embed-text-v2:0"

# Bedrock region (optional - defaults to aws_region if not specified)
# Uncomment if you need Bedrock in a different region
# bedrock_region = "us-west-2"
